global:
  scrape_interval: 15s

scrape_configs:
  # Custom roary metrics
  - job_name: 'roary'
    static_configs:
      - targets: ['nginx:80']
    metrics_path: '/metrics'

  # Node Exporter system metrics
  - job_name: 'node-exporter'
    static_configs:
      - targets: ['node-exporter:9100']
